name: Label PRs

on:
  pull_request_target:
    types: [opened]

jobs:
  label:
    runs-on: ubuntu-latest

    steps:
      - uses: nodejs/node-pr-labeler@d5c37475593a54a55415bc75a8f710d281c46587
        with:
          configuration-path: .github/pr-labels.yml
