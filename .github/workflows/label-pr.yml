name: Label PRs

on:
  pull_request_target:
    types: [opened]

jobs:
  label:
    runs-on: ubuntu-latest

    steps:
      - uses: nodejs/node-pr-labeler@71eb21029b84101f73f0bd54fb0e10886454e050
        with:
          configuration-path: .github/pr-labels.yml
