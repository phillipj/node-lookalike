name: Label PRs

on:
  pull_request_target:
    types: [opened]

jobs:
  label:
    runs-on: ubuntu-latest

    steps:
      - uses: nodejs/node-pr-labeler@3088344f9fd024300e2cae8c9fa2145d86a65d9d
        with:
          configuration-path: .github/pr-labels.yml
